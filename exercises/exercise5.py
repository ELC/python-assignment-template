"""Exercise Description: Returns the sum of two numbers"""


def division(a: float, b: float) -> float:
    """Returns the quotient of two floats

    Parameters
    ----------
    a : float
        Dividend
    b : float
        Divisor

    Returns
    -------
    float
        Quotient of the division of a and b

    Raises
    -------
    ValueError
        The divisor `b` cannot be zero
    """
    return a
